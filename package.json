{
  "name": "termine",
  "private": true,
  "version": "0.1.0",
  "description": "Self-hosted anonymous persistent messaging inbox",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter ./apps/* run dev",
    "dev:server": "pnpm --filter @termine/server run dev",
    "dev:web": "pnpm --filter @termine/web run dev",
    "build": "pnpm --filter @termine/crypto run build && pnpm --filter @termine/shared run build && pnpm --filter @termine/server run build && pnpm --filter @termine/web run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "db:migrate": "pnpm --filter @termine/server run db:migrate",
    "db:generate": "pnpm --filter @termine/server run db:generate"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@typescript-eslint/eslint-plugin": "^8.19.0",
    "@typescript-eslint/parser": "^8.19.0",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2"
  },
  "packageManager": "pnpm@9.15.1"
}
