# Local development environment for apps/server.
# For a self-hosted deployment, use the root .env.example with Docker Compose instead.

DATABASE_URL=postgresql://termine:termine@localhost:5432/termine
SESSION_SECRET=dev-only-secret-generate-a-real-one-with-openssl-rand-hex-32
PUBLIC_URL=http://localhost:5173
NODE_ENV=development
PORT=3000
